# Базовый образ: официальный Node.js
FROM node:22

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем package.json и package-lock.json (если есть)
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем весь остальной код (контролируя, что попадёт в образ)
COPY . .

# Открываем порт, на котором будет слушать сервер
EXPOSE 3000

# Запускаем приложение (предполагается, что в package.json есть скрипт "start")
CMD ["npm", "start"]
